@using TravelTripProje.Models.Sınıflar
@model List<Blog>

<div class="box1-left">

    @foreach (var x in Model)
    {<div class="bot-img">
            <img src="@x.BlogImage" />
        </div>
        <div class="bot-desc1">
            <p>@x.Baslik</p>
        </div>
        <div class="clear"></div>
    }


</div>